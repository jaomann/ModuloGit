using System;

namespace ModuloGit
{
    class Program
    {
        static void Main(string[] args)
        {
            do
            {
                Console.WriteLine("Digite duas coordenadas(X e Y)");
                int x = int.Parse(Console.ReadLine());
                int y = int.Parse(Console.ReadLine());
                if (x > 0) && ()
                {
                    Console.WriteLine("Quadrante 1");

                };
                    

            }
            while (x != 0) || (y != 0);
        }
    }
}
